<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal XML theme — only sets up the Activity window chrome.
         All visual design is handled by ApexTheme (Compose MaterialTheme). -->
    <style name="Theme.ApexAdDemo" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
