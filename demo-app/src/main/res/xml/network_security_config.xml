<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext HTTP is permitted ONLY for local dev hosts: the Apex ad server
     and demand platform run over plain http during development. 10.0.2.2 is
     the host machine as seen from the Android emulator; the 192.168.2.x
     entries are this dev machine's LAN addresses for physical-device testing.
     All other traffic (Sentry, real landing pages, production ad server)
     still requires HTTPS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.2.108</domain>
        <domain includeSubdomains="false">192.168.2.109</domain>
    </domain-config>
</network-security-config>
